#!/usr/bin/with-contenv bashio

if bashio::config.true 'Start Librespot?'; then
  bashio::log.info "Starting Librespot Server 2"

  exec /usr/bin/librespot \
  --name 'Snapcast Group 2' \
  --device-type avr \
  --backend pipe \
  --device /tmp/snap_librespot2 \
  --bitrate 160 \
  --disable-audio-cache \
  --enable-volume-normalisation \
  --volume-ctrl linear \
  --initial-volume=100 \
  >/dev/null 2>&1
fi
