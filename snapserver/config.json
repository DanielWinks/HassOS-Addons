{
  "name": "Snapserver",
  "version": "1.6.2",
  "slug": "snapserver_hassio",
  "description": "Snapserver for Home Assistant, with Librespot and REST TTS",
  "image": "dwinks/snapserver-{arch}",
  "arch": ["armhf", "armv7", "aarch64", "amd64", "i386"],
  "startup": "application",
  "host_network": true,
  "host_dbus": true,
  "boot": "auto",
  "options": {
    "settings": []
  },
  "schema": {
    "settings": [
      {
        "name": "str",
        "value": "str"
      }
    ]
  }
}
